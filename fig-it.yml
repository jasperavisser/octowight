postgres:
  image: postgres:9.2
# TODO: database needs to be initialized with events table & test data
rowchangeeventchannel:
  image: row-change-event-channel:latest
  links:
  - postgres
  environment:
    postgres.hostname: postgres
    postgres.port: 5432
    postgres.username: postgres
    postgres.database: postgres
