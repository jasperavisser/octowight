apply plugin: "spring-boot"
apply from: "../docker.gradle"

dependencies {
    compile project(":event-consumer")
    compile "org.aspectj:aspectjweaver"
    testCompile project(":spring-scala-mock")
}
